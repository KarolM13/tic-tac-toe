# Konfiguracja bazy danych
# 
# Dla SQLite (domyślna, nie wymaga instalacji):
# DATABASE_URL=sqlite:///games.db
#
# Dla PostgreSQL (produkcyjna):
# DATABASE_URL=postgresql://username:password@localhost:5432/tictactoe_db
#
# Przykład dla PostgreSQL w chmurze (Heroku, Railway):
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

DATABASE_URL=sqlite:///games.db

# Konfiguracja SMTP (opcjonalna - jeśli chcesz wysyłać emaile)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SENDER_EMAIL=your-email@gmail.com
# SENDER_PASSWORD=your-app-password
